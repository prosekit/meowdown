name: WebKit Crash Research

on:
  push:
    branches: [research/webkit-birpc-crash]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repro:
    runs-on: macos-latest
    timeout-minutes: 45
    name: repro-${{ matrix.mode }}-${{ matrix.round }}

    strategy:
      fail-fast: false
      matrix:
        mode: [starved]
        round: [1, 2, 3, 4]
        include:
          - mode: starved-amplified
            round: 1
          - mode: starved-amplified
            round: 2

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: ./.github/actions/setup

      - name: Install WebKit
        run: pnpm run test:install:webkit

      - name: Start memory sampler
        run: |
          mkdir -p "$RUNNER_TEMP/research-out"
          nohup bash .github/research/sample-memory.sh > "$RUNNER_TEMP/research-out/memory-samples.log" 2>&1 &

      - name: Start CPU burners
        if: startsWith(matrix.mode, 'starved')
        run: |
          nohup yes > /dev/null 2>&1 &
          nohup yes > /dev/null 2>&1 &
          nohup yes > /dev/null 2>&1 &

      - name: Start memory hog
        if: startsWith(matrix.mode, 'starved')
        run: nohup node .github/research/memory-hog.mjs 1.5 500 > "$RUNNER_TEMP/research-out/memory-hog.log" 2>&1 &

      - name: Run tests in a loop
        run: bash .github/research/run-loop.sh
        env:
          RESEARCH_FUZZ_NUM_RUNS: ${{ matrix.mode == 'starved-amplified' && '300000' || '' }}
          RESEARCH_ITERATIONS: '2'

      - name: Collect evidence
        if: always()
        run: bash .github/research/collect-evidence.sh

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: evidence-${{ matrix.mode }}-${{ matrix.round }}
          path: ${{ runner.temp }}/research-out
          include-hidden-files: true
