import { TaskList as TaskListComponent } from './task-list.tsx'

export default {
  component: TaskListComponent,
}

// REVIEW: you have 3 *.stories.ts files. Each of them now has one single story. I do not want it. I want one single stories.ts file that contains all 3 stories.

export const TaskList = {}
