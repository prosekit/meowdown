# The long document

A stress-test preset: many blocks, many inline marks, and enough text to make serialization and rendering costs visible.

## Section 1: field notes

Entry 1 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-1) and a #note-1 tag.

- First observation of section 1
  - A nested detail
  - Another nested detail
- Second observation
- Third observation with ~~struck~~ text

Closing paragraph 1: the inline math $x_{1} = 1^2$ keeps the math pipeline warm.

## Section 2: field notes

Entry 2 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-2) and a #note-2 tag.

+ [ ] Follow up on section 2
+ [x] Archive section 1
- [ ] Cross-check with [[Daily journal]]

Closing paragraph 2: the inline math $x_{2} = 2^2$ keeps the math pipeline warm.

## Section 3: field notes

Entry 3 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-3) and a #note-3 tag.

| metric | value | delta |
| ------ | ----- | ----- |
| alpha  | 9 | +3 |
| beta   | 21 | -3 |

Closing paragraph 3: the inline math $x_{3} = 3^2$ keeps the math pipeline warm.

## Section 4: field notes

Entry 4 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-4) and a #note-4 tag.

> Section 4 quote: writing a long document is mostly writing many short ones.

Closing paragraph 4: the inline math $x_{4} = 4^2$ keeps the math pipeline warm.

## Section 5: field notes

Entry 5 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-5) and a #note-5 tag.

```typescript
export function section5(): number {
  return 5 * 42
}
```

Closing paragraph 5: the inline math $x_{5} = 5^2$ keeps the math pipeline warm.

## Section 6: field notes

Entry 6 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-6) and a #note-6 tag.

- First observation of section 6
  - A nested detail
  - Another nested detail
- Second observation
- Third observation with ~~struck~~ text

Closing paragraph 6: the inline math $x_{6} = 6^2$ keeps the math pipeline warm.

## Section 7: field notes

Entry 7 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-7) and a #note-7 tag.

+ [ ] Follow up on section 7
+ [x] Archive section 6
- [ ] Cross-check with [[Daily journal]]

Closing paragraph 7: the inline math $x_{7} = 7^2$ keeps the math pipeline warm.

## Section 8: field notes

Entry 8 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-8) and a #note-8 tag.

| metric | value | delta |
| ------ | ----- | ----- |
| alpha  | 24 | +8 |
| beta   | 56 | -8 |

Closing paragraph 8: the inline math $x_{8} = 8^2$ keeps the math pipeline warm.

## Section 9: field notes

Entry 9 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-9) and a #note-9 tag.

> Section 9 quote: writing a long document is mostly writing many short ones.

Closing paragraph 9: the inline math $x_{9} = 9^2$ keeps the math pipeline warm.

## Section 10: field notes

Entry 10 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-10) and a #note-10 tag.

```typescript
export function section10(): number {
  return 10 * 42
}
```

Closing paragraph 10: the inline math $x_{10} = 10^2$ keeps the math pipeline warm.

## Section 11: field notes

Entry 11 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-11) and a #note-11 tag.

- First observation of section 11
  - A nested detail
  - Another nested detail
- Second observation
- Third observation with ~~struck~~ text

Closing paragraph 11: the inline math $x_{11} = 11^2$ keeps the math pipeline warm.

## Section 12: field notes

Entry 12 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-12) and a #note-12 tag.

+ [ ] Follow up on section 12
+ [x] Archive section 11
- [ ] Cross-check with [[Daily journal]]

Closing paragraph 12: the inline math $x_{12} = 12^2$ keeps the math pipeline warm.

## Section 13: field notes

Entry 13 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-13) and a #note-13 tag.

| metric | value | delta |
| ------ | ----- | ----- |
| alpha  | 39 | +13 |
| beta   | 91 | -13 |

Closing paragraph 13: the inline math $x_{13} = 13^2$ keeps the math pipeline warm.

## Section 14: field notes

Entry 14 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-14) and a #note-14 tag.

> Section 14 quote: writing a long document is mostly writing many short ones.

Closing paragraph 14: the inline math $x_{14} = 14^2$ keeps the math pipeline warm.

## Section 15: field notes

Entry 15 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-15) and a #note-15 tag.

```typescript
export function section15(): number {
  return 15 * 42
}
```

Closing paragraph 15: the inline math $x_{15} = 15^2$ keeps the math pipeline warm.

## Section 16: field notes

Entry 16 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-16) and a #note-16 tag.

- First observation of section 16
  - A nested detail
  - Another nested detail
- Second observation
- Third observation with ~~struck~~ text

Closing paragraph 16: the inline math $x_{16} = 16^2$ keeps the math pipeline warm.

## Section 17: field notes

Entry 17 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-17) and a #note-17 tag.

+ [ ] Follow up on section 17
+ [x] Archive section 16
- [ ] Cross-check with [[Daily journal]]

Closing paragraph 17: the inline math $x_{17} = 17^2$ keeps the math pipeline warm.

## Section 18: field notes

Entry 18 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-18) and a #note-18 tag.

| metric | value | delta |
| ------ | ----- | ----- |
| alpha  | 54 | +18 |
| beta   | 126 | -18 |

Closing paragraph 18: the inline math $x_{18} = 18^2$ keeps the math pipeline warm.

## Section 19: field notes

Entry 19 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-19) and a #note-19 tag.

> Section 19 quote: writing a long document is mostly writing many short ones.

Closing paragraph 19: the inline math $x_{19} = 19^2$ keeps the math pipeline warm.

## Section 20: field notes

Entry 20 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-20) and a #note-20 tag.

```typescript
export function section20(): number {
  return 20 * 42
}
```

Closing paragraph 20: the inline math $x_{20} = 20^2$ keeps the math pipeline warm.

## Section 21: field notes

Entry 21 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-21) and a #note-21 tag.

- First observation of section 21
  - A nested detail
  - Another nested detail
- Second observation
- Third observation with ~~struck~~ text

Closing paragraph 21: the inline math $x_{21} = 21^2$ keeps the math pipeline warm.

## Section 22: field notes

Entry 22 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-22) and a #note-22 tag.

+ [ ] Follow up on section 22
+ [x] Archive section 21
- [ ] Cross-check with [[Daily journal]]

Closing paragraph 22: the inline math $x_{22} = 22^2$ keeps the math pipeline warm.

## Section 23: field notes

Entry 23 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-23) and a #note-23 tag.

| metric | value | delta |
| ------ | ----- | ----- |
| alpha  | 69 | +23 |
| beta   | 161 | -23 |

Closing paragraph 23: the inline math $x_{23} = 23^2$ keeps the math pipeline warm.

## Section 24: field notes

Entry 24 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-24) and a #note-24 tag.

> Section 24 quote: writing a long document is mostly writing many short ones.

Closing paragraph 24: the inline math $x_{24} = 24^2$ keeps the math pipeline warm.

## Section 25: field notes

Entry 25 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-25) and a #note-25 tag.

```typescript
export function section25(): number {
  return 25 * 42
}
```

Closing paragraph 25: the inline math $x_{25} = 25^2$ keeps the math pipeline warm.

## Section 26: field notes

Entry 26 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-26) and a #note-26 tag.

- First observation of section 26
  - A nested detail
  - Another nested detail
- Second observation
- Third observation with ~~struck~~ text

Closing paragraph 26: the inline math $x_{26} = 26^2$ keeps the math pipeline warm.

## Section 27: field notes

Entry 27 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-27) and a #note-27 tag.

+ [ ] Follow up on section 27
+ [x] Archive section 26
- [ ] Cross-check with [[Daily journal]]

Closing paragraph 27: the inline math $x_{27} = 27^2$ keeps the math pipeline warm.

## Section 28: field notes

Entry 28 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-28) and a #note-28 tag.

| metric | value | delta |
| ------ | ----- | ----- |
| alpha  | 84 | +28 |
| beta   | 196 | -28 |

Closing paragraph 28: the inline math $x_{28} = 28^2$ keeps the math pipeline warm.

## Section 29: field notes

Entry 29 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-29) and a #note-29 tag.

> Section 29 quote: writing a long document is mostly writing many short ones.

Closing paragraph 29: the inline math $x_{29} = 29^2$ keeps the math pipeline warm.

## Section 30: field notes

Entry 30 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-30) and a #note-30 tag.

```typescript
export function section30(): number {
  return 30 * 42
}
```

Closing paragraph 30: the inline math $x_{30} = 30^2$ keeps the math pipeline warm.

## Section 31: field notes

Entry 31 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-31) and a #note-31 tag.

- First observation of section 31
  - A nested detail
  - Another nested detail
- Second observation
- Third observation with ~~struck~~ text

Closing paragraph 31: the inline math $x_{31} = 31^2$ keeps the math pipeline warm.

## Section 32: field notes

Entry 32 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-32) and a #note-32 tag.

+ [ ] Follow up on section 32
+ [x] Archive section 31
- [ ] Cross-check with [[Daily journal]]

Closing paragraph 32: the inline math $x_{32} = 32^2$ keeps the math pipeline warm.

## Section 33: field notes

Entry 33 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-33) and a #note-33 tag.

| metric | value | delta |
| ------ | ----- | ----- |
| alpha  | 99 | +33 |
| beta   | 231 | -33 |

Closing paragraph 33: the inline math $x_{33} = 33^2$ keeps the math pipeline warm.

## Section 34: field notes

Entry 34 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-34) and a #note-34 tag.

> Section 34 quote: writing a long document is mostly writing many short ones.

Closing paragraph 34: the inline math $x_{34} = 34^2$ keeps the math pipeline warm.

## Section 35: field notes

Entry 35 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-35) and a #note-35 tag.

```typescript
export function section35(): number {
  return 35 * 42
}
```

Closing paragraph 35: the inline math $x_{35} = 35^2$ keeps the math pipeline warm.

## Section 36: field notes

Entry 36 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-36) and a #note-36 tag.

- First observation of section 36
  - A nested detail
  - Another nested detail
- Second observation
- Third observation with ~~struck~~ text

Closing paragraph 36: the inline math $x_{36} = 36^2$ keeps the math pipeline warm.

## Section 37: field notes

Entry 37 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-37) and a #note-37 tag.

+ [ ] Follow up on section 37
+ [x] Archive section 36
- [ ] Cross-check with [[Daily journal]]

Closing paragraph 37: the inline math $x_{37} = 37^2$ keeps the math pipeline warm.

## Section 38: field notes

Entry 38 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-38) and a #note-38 tag.

| metric | value | delta |
| ------ | ----- | ----- |
| alpha  | 114 | +38 |
| beta   | 266 | -38 |

Closing paragraph 38: the inline math $x_{38} = 38^2$ keeps the math pipeline warm.

## Section 39: field notes

Entry 39 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-39) and a #note-39 tag.

> Section 39 quote: writing a long document is mostly writing many short ones.

Closing paragraph 39: the inline math $x_{39} = 39^2$ keeps the math pipeline warm.

## Section 40: field notes

Entry 40 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-40) and a #note-40 tag.

```typescript
export function section40(): number {
  return 40 * 42
}
```

Closing paragraph 40: the inline math $x_{40} = 40^2$ keeps the math pipeline warm.

## Section 41: field notes

Entry 41 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-41) and a #note-41 tag.

- First observation of section 41
  - A nested detail
  - Another nested detail
- Second observation
- Third observation with ~~struck~~ text

Closing paragraph 41: the inline math $x_{41} = 41^2$ keeps the math pipeline warm.

## Section 42: field notes

Entry 42 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-42) and a #note-42 tag.

+ [ ] Follow up on section 42
+ [x] Archive section 41
- [ ] Cross-check with [[Daily journal]]

Closing paragraph 42: the inline math $x_{42} = 42^2$ keeps the math pipeline warm.

## Section 43: field notes

Entry 43 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-43) and a #note-43 tag.

| metric | value | delta |
| ------ | ----- | ----- |
| alpha  | 129 | +43 |
| beta   | 301 | -43 |

Closing paragraph 43: the inline math $x_{43} = 43^2$ keeps the math pipeline warm.

## Section 44: field notes

Entry 44 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-44) and a #note-44 tag.

> Section 44 quote: writing a long document is mostly writing many short ones.

Closing paragraph 44: the inline math $x_{44} = 44^2$ keeps the math pipeline warm.

## Section 45: field notes

Entry 45 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-45) and a #note-45 tag.

```typescript
export function section45(): number {
  return 45 * 42
}
```

Closing paragraph 45: the inline math $x_{45} = 45^2$ keeps the math pipeline warm.

## Section 46: field notes

Entry 46 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-46) and a #note-46 tag.

- First observation of section 46
  - A nested detail
  - Another nested detail
- Second observation
- Third observation with ~~struck~~ text

Closing paragraph 46: the inline math $x_{46} = 46^2$ keeps the math pipeline warm.

## Section 47: field notes

Entry 47 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-47) and a #note-47 tag.

+ [ ] Follow up on section 47
+ [x] Archive section 46
- [ ] Cross-check with [[Daily journal]]

Closing paragraph 47: the inline math $x_{47} = 47^2$ keeps the math pipeline warm.

## Section 48: field notes

Entry 48 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-48) and a #note-48 tag.

| metric | value | delta |
| ------ | ----- | ----- |
| alpha  | 144 | +48 |
| beta   | 336 | -48 |

Closing paragraph 48: the inline math $x_{48} = 48^2$ keeps the math pipeline warm.

## Section 49: field notes

Entry 49 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-49) and a #note-49 tag.

> Section 49 quote: writing a long document is mostly writing many short ones.

Closing paragraph 49: the inline math $x_{49} = 49^2$ keeps the math pipeline warm.

## Section 50: field notes

Entry 50 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-50) and a #note-50 tag.

```typescript
export function section50(): number {
  return 50 * 42
}
```

Closing paragraph 50: the inline math $x_{50} = 50^2$ keeps the math pipeline warm.

## Section 51: field notes

Entry 51 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-51) and a #note-51 tag.

- First observation of section 51
  - A nested detail
  - Another nested detail
- Second observation
- Third observation with ~~struck~~ text

Closing paragraph 51: the inline math $x_{51} = 51^2$ keeps the math pipeline warm.

## Section 52: field notes

Entry 52 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-52) and a #note-52 tag.

+ [ ] Follow up on section 52
+ [x] Archive section 51
- [ ] Cross-check with [[Daily journal]]

Closing paragraph 52: the inline math $x_{52} = 52^2$ keeps the math pipeline warm.

## Section 53: field notes

Entry 53 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-53) and a #note-53 tag.

| metric | value | delta |
| ------ | ----- | ----- |
| alpha  | 159 | +53 |
| beta   | 371 | -53 |

Closing paragraph 53: the inline math $x_{53} = 53^2$ keeps the math pipeline warm.

## Section 54: field notes

Entry 54 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-54) and a #note-54 tag.

> Section 54 quote: writing a long document is mostly writing many short ones.

Closing paragraph 54: the inline math $x_{54} = 54^2$ keeps the math pipeline warm.

## Section 55: field notes

Entry 55 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-55) and a #note-55 tag.

```typescript
export function section55(): number {
  return 55 * 42
}
```

Closing paragraph 55: the inline math $x_{55} = 55^2$ keeps the math pipeline warm.

## Section 56: field notes

Entry 56 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-56) and a #note-56 tag.

- First observation of section 56
  - A nested detail
  - Another nested detail
- Second observation
- Third observation with ~~struck~~ text

Closing paragraph 56: the inline math $x_{56} = 56^2$ keeps the math pipeline warm.

## Section 57: field notes

Entry 57 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-57) and a #note-57 tag.

+ [ ] Follow up on section 57
+ [x] Archive section 56
- [ ] Cross-check with [[Daily journal]]

Closing paragraph 57: the inline math $x_{57} = 57^2$ keeps the math pipeline warm.

## Section 58: field notes

Entry 58 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-58) and a #note-58 tag.

| metric | value | delta |
| ------ | ----- | ----- |
| alpha  | 174 | +58 |
| beta   | 406 | -58 |

Closing paragraph 58: the inline math $x_{58} = 58^2$ keeps the math pipeline warm.

## Section 59: field notes

Entry 59 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-59) and a #note-59 tag.

> Section 59 quote: writing a long document is mostly writing many short ones.

Closing paragraph 59: the inline math $x_{59} = 59^2$ keeps the math pipeline warm.

## Section 60: field notes

Entry 60 starts with **bold intent**, drifts into *italic asides*, and ends with `inline code` plus a [link](https://example.com/section-60) and a #note-60 tag.

```typescript
export function section60(): number {
  return 60 * 42
}
```

Closing paragraph 60: the inline math $x_{60} = 60^2$ keeps the math pipeline warm.
