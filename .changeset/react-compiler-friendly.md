---
'@meowdown/react': patch
---

Rewrite the imperative handle getter, the `try/finally` around `dispatch`, and the link hover handler registration so React Compiler can compile `MeowdownEditor`, `ProseKitEditor`, and `LinkMenu`.
