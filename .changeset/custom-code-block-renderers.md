---
"@meowdown/core": minor
"@meowdown/react": minor
---

Add `renderCodeBlock` so a host can render selected fenced code blocks, and `insertMarkdown(..., { selection: 'after-block' })` to leave the caret in the following paragraph.
